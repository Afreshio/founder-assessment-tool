[build]
  command = "npm run build"
  publish = "dist"

# API redirect - must come before the catch-all redirect
[[redirects]]
  from = "/api/send-report"
  to = "/.netlify/functions/send-report"
  status = 200

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

